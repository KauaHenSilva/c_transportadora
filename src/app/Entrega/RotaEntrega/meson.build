lib_rota_entrega = static_library(
  'rota_entrega',
  'rota_entrega.c',
  include_directories: [
    inc_struct_entrega,
    inc_rota_segunda_entrega,
    inc_print_struct,
    inc_utils,
    inc_pacote,
    inc_historico,
  ],
  install: true,
)