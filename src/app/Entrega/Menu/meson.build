lib_menu = static_library(
  'menu',
  'menu.c',
  include_directories: [
    inc_struct_entrega,
    inc_print_struct,
    inc_rota_entrega,
    inc_cliente_entrega,
    inc_utils,
  ],
  install: true,
)